package de.uniluebeck.iti.rteasy;
import java.util.*;

public class SwitchTable {
  /*
  class ListEntry {
    public CircuitPortReference source;
    public BitRange target;
    Entry(CircuitPortReference toSource, BitRange toTarget) {
      source = toSource; target = toTarget;
    }
  }

  /* entries in switch table are lists of source CircuitPortReferences
   * ordered by their beginning in the target
   */
  /*
  private Hashtable table;

  SwitchTable() {
    table = new Hashtable();
  }

  void include(int signal, CircuitPort sourcePort, int portIdx, int targetIdx) {
    Integer key = new Integer(signal);
    ArrayList refList;
    if(table.containsKey(key)) {
      refList = (ArrayList) table.get(key);
    */  
}
